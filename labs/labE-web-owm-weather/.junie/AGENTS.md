# Guidelines (Weather App Lab)

- Keep the OpenWeatherMap API key on the server side only.
- Use the existing Express `/api/weather` proxy from browser code.
- Prefer small React components and typed client helpers.
- Show clear loading, empty, and error states.
- Add Testing Library tests for user-visible behavior rather than implementation details.
- Run `npm test` after UI or API wrapper changes.

